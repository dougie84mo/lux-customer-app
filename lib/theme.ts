import { MD3LightTheme, MD3DarkTheme } from 'react-native-paper';

// Paper themes wired so we can swap with system color scheme. Keep brand
// tokens in this file once we have them — for now use Paper defaults.
export const lightTheme = {
  ...MD3LightTheme,
};

export const darkTheme = {
  ...MD3DarkTheme,
};
